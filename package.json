{
  "name": "herdr-with-pi",
  "version": "0.1.0",
  "description": "herdr-with-pi: strategy layer for multi-agent orchestration with Herdr — herdr_* tools (derived from @ogulcancelik/pi-herdr) plus the herdr-with-pi skill and subagent profiles. `pi -e .` from this directory loads tools + skill.",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pi-extension",
    "herdr",
    "herdr-with-pi",
    "subagent",
    "multi-agent",
    "delegation"
  ],
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.80.10",
    "@earendil-works/pi-coding-agent": "^0.80.10",
    "@earendil-works/pi-tui": "^0.80.10",
    "@sinclair/typebox": "^0.34.0",
    "@types/node": "^22.0.0"
  },
  "scripts": {
    "test": "bun test"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "files": [
    "index.ts",
    "SKILL.md",
    "agents/",
    "README.md",
    "README_zh.md"
  ]
}
